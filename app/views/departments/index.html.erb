<p id="notice"><%= notice %></p>

<%= link_to 'Home', root_path %> <span> | </span>
<%= link_to 'Depertments', departments_path %><span> | </span>
<%= link_to 'Sections', sections_path %><span> | </span>
<%= link_to 'Subsections', subsections_path %><span> | </span>
<%= link_to 'Contact Us', contactus_path %><span> | </span>
<%= link_to 'About', about_path %>

<h1>Departments</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Shortname</th>
      <th>Link</th>
      <th>Description</th>
      <th>Position</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr>
        <td><%= department.name %></td>
        <td><%= department.shortname %></td>
        <td><%= department.link %></td>
        <td><%= department.description %></td>
        <td><%= department.position %></td>
        <td><%= department.status %></td>
        <td><%= link_to 'Show', department %></td>
        <td><%= link_to 'Edit', edit_department_path(department) %></td>
        <td><%= link_to 'Destroy', department, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Department', new_department_path %>
